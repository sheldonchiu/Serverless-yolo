version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  yolo-openfaas:
    lang: dockerfile
    handler: ./yolo
    image: sheldonchiu/yolo-openfaas:latest
    environment:
      redis: openfaas-redis-master

